prepare-commit-msg:
  commands:
    commitizen:
      interactive: true
      run: pnpm cz --hook
      env:
        LEFTHOOK: 0

commit-msg:
  commands:
    "lint commit message":
      run: pnpm commitlint --edit {1}
