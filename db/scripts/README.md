# Scripts

Place F1DB restore/import scripts here.

Expected responsibilities:

- Restore the F1DB PostgreSQL dump into a source database or raw schema.
- Load or expose source tables for dbt staging models.
- Record refresh metadata in `effone.refresh_runs`.

